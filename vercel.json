{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
git add vercel.json
git commit -m "fix: add SPA rewrite fallback in vercel.json"
git push origin main
